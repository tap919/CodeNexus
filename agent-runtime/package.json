{
  "name": "@codenexus/agent-runtime",
  "version": "1.0.0",
  "description": "CodeNexus Agent Runtime — AI coding agent orchestration",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@types/uuid": "^10.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.2.4"
  }
}
